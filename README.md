# fresh_cart

This repository contains both applications for the Fresh Cart project:

- `frontend/` - Flutter frontend application
- `backend/` - Backend service folder

## Repository Structure

```text
fresh_cart/
|-- frontend/
`-- backend/
```

## Next Steps

1. Build and maintain the Flutter app inside `frontend/`.
2. Add your backend code inside `backend/`.
